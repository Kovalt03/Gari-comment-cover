# macOS
.DS_Store
._*

# 의존성 / 빌드
node_modules/
dist/
build/
*.tsbuildinfo

# 모델 가중치 (용량이 커서 저장소에 넣지 않는다. 받는 법은 docs/model_selection.md 참고)
models/
*.onnx
*.onnx_data
*.bin
*.safetensors

# 수집한 댓글과 라벨링 결과. 다른 사람이 쓴 글이라 저장소에 올리지 않는다.
# 공개하는 것은 평가 리포트의 수치뿐이다 (docs/PRIVACY.md 8절)
data/

# 로그 / 측정 산출물
*.log
bench/out/

# 개인 계획 / 학습 문서 (저장소에 올리지 않는다)
docs/plan/
docs/study/

# 로컬 설정
.env
.env.*
!.env.example
.vscode/
.idea/
